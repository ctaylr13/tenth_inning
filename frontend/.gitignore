# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# yarn.lock is committed on purpose -- it is what makes a build reproducible
# and what `yarn install --frozen-lockfile` checks against in CI.
# package-lock.json stays ignored above so npm can't quietly become a second
# source of truth.