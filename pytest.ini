[pytest]
# server.py, errors.py and error_handlers.py live at the repo root. Bare
# `pytest` does not put the working directory on sys.path (only `python -m
# pytest` does), so without this the imports work locally and fail in CI.
pythonpath = .
testpaths = tests
